# 04. DEM 程序化地形生成

## 对应图片
- ../images/03_dem_terrain.svg

## 讲解目标
当前实际运行路径是 generateDemPrototype()，用于生成高度、水文和地表属性。

## 关键代码位置
- src/PlanetProceduralData.cpp

## 讲解流程
1. PlanetProceduralData::generate()
2. clamp faceResolution
3. 进入 generateDemPrototype()
4. 为 6 个 face 分配高度、水深、侵蚀等数组
5. 遍历每个 texel 并计算 sphereDir
6. fBM / ridgedFbm 生成大陆和山脉
7. 记录 height、uplift、landMask、preErosionHeight
8. 接缝融合、水文侵蚀、气候计算
9. buildTerrainChunks() 烘焙地形块

## 可直接读的口播稿
> 老师好，这一页我讲的是「DEM 程序化地形生成」。这部分主要想说明：当前实际运行路径是 generateDemPrototype()，用于生成高度、水文和地表属性。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 这一页要特别讲清楚当前真实代码路径。PlanetProceduralData::generate() 进入 generateDemPrototype() 后直接 return，因此现在运行的是 DEM 原型路径。

> DEM 可以理解为数字高程模型。代码给六个 cube face 分配多张数组，包括高度、水深、侵蚀、温度、湿度、河道和材质权重。

> 每个 texel 通过球面方向采样多层噪声。低频噪声决定大陆轮廓，中频和高频噪声塑造山脉、山脊和局部起伏。

> 得到基础地形后，代码再进行接缝修正、水文侵蚀和气候字段计算。

> 最后 buildTerrainChunks() 把 DEM 数据烘焙成可渲染的地形块，这一步把生成阶段和渲染阶段连接起来。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「PlanetProceduralData::generate()」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「clamp faceResolution」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「进入 generateDemPrototype()」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「为 6 个 face 分配高度、水深、侵蚀等数组」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「遍历每个 texel 并计算 sphereDir」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「fBM / ridgedFbm 生成大陆和山脉」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「记录 height、uplift、landMask、preErosionHeight」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「接缝融合、水文侵蚀、气候计算」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 9 步是「buildTerrainChunks() 烘焙地形块」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：不要把后面保留的旧版分模块生成代码说成当前主路径；它现在是保留代码，当前入口已经提前返回。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。