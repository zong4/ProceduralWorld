# 17. 高度诊断工具

## 对应图片
- ../images/08_frame_debug.svg

## 讲解目标
独立命令行工具用于检查高度范围和生成质量。

## 关键代码位置
- tools/TerrainHeightDiagnostics.cpp
- xmake.lua

## 讲解流程
1. 运行 TerrainHeightDiagnostics
2. 构造 PlanetProceduralSettings
3. 调用 PlanetProceduralData::generate()
4. 遍历 6 个 face 的 height 数据
5. 统计 min / max / average
6. 检查水体和陆地比例
7. 输出诊断结果
8. 辅助调整生成参数

## 可直接读的口播稿
> 老师好，这一页我讲的是「高度诊断工具」。这部分主要想说明：独立命令行工具用于检查高度范围和生成质量。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 高度诊断工具是辅助开发用的，不需要打开完整渲染窗口。

> 它直接生成一份程序化数据，然后统计高度范围、平均值、水陆比例等信息。

> 这样可以快速发现生成参数是否异常，比如高度过平、海洋过多或陆地过少。

> 对答辩来说，这说明项目有验证工具，不完全依赖肉眼看画面。

> 如果后续继续扩展，也可以把更多指标加入这个工具，比如河道数量、侵蚀强度分布和材质覆盖率。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「运行 TerrainHeightDiagnostics」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「构造 PlanetProceduralSettings」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「调用 PlanetProceduralData::generate()」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「遍历 6 个 face 的 height 数据」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「统计 min / max / average」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「检查水体和陆地比例」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「输出诊断结果」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「辅助调整生成参数」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：这一页可以作为工程完整性的补充：有主程序，也有独立诊断工具。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。