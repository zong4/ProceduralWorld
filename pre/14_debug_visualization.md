# 14. 调试、可视化与性能面板

## 对应图片
- ../images/08_frame_debug.svg

## 讲解目标
用 ImGui 暴露参数和调试视图，帮助解释生成结果。

## 关键代码位置
- src/main.cpp
- src/PlanetRenderer.cpp

## 讲解流程
1. Render panel 展示地形、海洋、大气、云参数
2. 用户切换 render mode / debug overlay
3. renderer 更新 shader uniform
4. 显示 hydrology、erosion、material 等调试层
5. Performance panel 统计帧时间和可见块
6. Ocean / cloud / baked chunk 参数实时反馈
7. Feature overlay 代码存在
8. 当前初次 DEM 路径和主 render 还未完整接通 overlay

## 可直接读的口播稿
> 老师好，这一页我讲的是「调试、可视化与性能面板」。这部分主要想说明：用 ImGui 暴露参数和调试视图，帮助解释生成结果。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 这个模块适合答辩现场展示，因为它能证明项目不是只跑一次结果，而是可以调参数、看中间层。

> Render panel 里可以控制地形表面、水文调试、海洋、大气、云和相机质量参数。

> 调试视图能显示河道、侵蚀、材质、LOD 等信息，用来解释为什么某个区域会形成山脉、河流或海岸。

> Performance panel 则显示帧时间、可见 chunk、海洋 patch 和云质量等数据。

> 需要诚实说明：特征线 overlay 的构建代码存在，但当前初次 DEM 生成路径会跳过，主 render 也没有完整调用；cache 加载路径可以重建部分数据。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「Render panel 展示地形、海洋、大气、云参数」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「用户切换 render mode / debug overlay」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「renderer 更新 shader uniform」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「显示 hydrology、erosion、material 等调试层」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「Performance panel 统计帧时间和可见块」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「Ocean / cloud / baked chunk 参数实时反馈」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「Feature overlay 代码存在」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「当前初次 DEM 路径和主 render 还未完整接通 overlay」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：这一页可以主动展示工程诚实性：哪些功能已实装，哪些是代码支持但当前路径未完全启用。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。