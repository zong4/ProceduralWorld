# 12. 大气散射 LUT 与天空

## 对应图片
- ../images/07_atmosphere_clouds.svg

## 讲解目标
预计算大气散射查找表，运行时快速渲染天空和远景。

## 关键代码位置
- src/PlanetRenderer.cpp
- shaders/atmosphere*.frag
- shaders/atmosphere*.vert

## 讲解流程
1. 根据大气参数计算 LUT signature
2. 参数变化时触发 precomputeAtmosphereLuts()
3. 计算 transmittance LUT
4. 计算 irradiance LUT
5. 计算 scattering LUT
6. 运行时 atmosphere.frag 采样 LUT
7. 根据太阳方向、视线和高度计算天空颜色
8. 与地形、海洋和云层合成

## 可直接读的口播稿
> 老师好，这一页我讲的是「大气散射 LUT 与天空」。这部分主要想说明：预计算大气散射查找表，运行时快速渲染天空和远景。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 大气散射如果每个像素都完整积分会很贵，所以项目采用 LUT 预计算。

> 当大气参数变化时，渲染器生成一个 signature，判断 LUT 是否需要重算。

> 预计算阶段会得到透射率、辐照度和散射相关纹理。

> 运行时 shader 通过查表快速得到天空颜色、地平线雾化和远景衰减。

> 这个模块的价值是把复杂物理近似前置，保证实时帧率。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「根据大气参数计算 LUT signature」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「参数变化时触发 precomputeAtmosphereLuts()」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「计算 transmittance LUT」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「计算 irradiance LUT」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「计算 scattering LUT」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「运行时 atmosphere.frag 采样 LUT」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「根据太阳方向、视线和高度计算天空颜色」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「与地形、海洋和云层合成」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：讲解时可以把 LUT 说成提前算好的表：用一点预处理换每帧渲染速度。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。