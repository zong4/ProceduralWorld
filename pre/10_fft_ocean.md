# 10. FFT 海浪

## 对应图片
- ../images/06_ocean_fft.svg

## 讲解目标
用频域海浪谱生成高度、法线、位移和 folding 纹理。

## 关键代码位置
- src/FFTOcean.cpp
- include/FFTOcean.h

## 讲解流程
1. initialize() 配置 cascades 和分辨率
2. buildInitialSpectrum() 生成 Phillips spectrum
3. 根据 wind、amplitude、gravity 初始化频域
4. update(time) 推进频域相位
5. IFFT 转回空间域
6. 计算 height / normal / displacement / folding
7. uploadTextures() 上传 GPU
8. ocean shader 采样形成动态波浪

## 可直接读的口播稿
> 老师好，这一页我讲的是「FFT 海浪」。这部分主要想说明：用频域海浪谱生成高度、法线、位移和 folding 纹理。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> FFT 海洋模块负责让水面真正动起来。

> 它先在频域生成初始海浪谱，参数包括风向、风速、振幅和重力等。

> 每一帧根据时间更新频域相位，再通过 IFFT 得到空间域波形。

> 输出不只有高度，还有法线、水平位移和 folding 信息。

> 这些纹理上传到 GPU 后，海洋 shader 就可以在不同距离采样不同 cascade，形成从近景波浪到远景起伏的连续效果。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「initialize() 配置 cascades 和分辨率」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「buildInitialSpectrum() 生成 Phillips spectrum」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「根据 wind、amplitude、gravity 初始化频域」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「update(time) 推进频域相位」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「IFFT 转回空间域」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「计算 height / normal / displacement / folding」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「uploadTextures() 上传 GPU」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「ocean shader 采样形成动态波浪」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：讲 FFT 时不用展开数学细节，重点说：频域生成复杂波形，IFFT 转成水面纹理。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。