# 09. 海洋 Patch LOD 与球面海面

## 对应图片
- ../images/06_ocean_fft.svg

## 讲解目标
在球面上动态选择海洋 patch，并用 tessellation 提升近处细节。

## 关键代码位置
- src/PlanetRenderer.cpp
- shaders/ocean.tesc
- shaders/ocean.tese
- shaders/ocean.frag

## 讲解流程
1. 从 6 个 cube face 创建 ocean root patch
2. collectVisibleOceanPatches() 遍历四叉树
3. 视锥裁剪排除不可见 patch
4. analyzePatchWaterCoverage() 判断水域比例
5. shouldSplitNode() 根据距离和误差细分
6. 生成可见 ocean patch 列表
7. tessellation shader 球面细分
8. fragment shader 计算水面颜色

## 可直接读的口播稿
> 老师好，这一页我讲的是「海洋 Patch LOD 与球面海面」。这部分主要想说明：在球面上动态选择海洋 patch，并用 tessellation 提升近处细节。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 海洋不是一张固定平面，而是围绕星球的球面 patch。

> 每个 cube face 有海洋根节点，运行时通过四叉树选择可见区域。

> 如果 patch 不在视野里，或者水域覆盖率太低，就可以跳过。

> 近处 patch 会继续细分，远处 patch 保持粗略，从而节省绘制成本。

> tessellation shader 负责把 patch 细分并贴合球面，fragment shader 再根据 FFT 波浪、反射、折射和水深计算颜色。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「从 6 个 cube face 创建 ocean root patch」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「collectVisibleOceanPatches() 遍历四叉树」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「视锥裁剪排除不可见 patch」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「analyzePatchWaterCoverage() 判断水域比例」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「shouldSplitNode() 根据距离和误差细分」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「生成可见 ocean patch 列表」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「tessellation shader 球面细分」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「fragment shader 计算水面颜色」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：可以把海洋 LOD 和地形 chunk LOD 对比：地形是 baked chunk，海洋是运行时 quadtree patch。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。