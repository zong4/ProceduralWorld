# 08. CPU 数据到 GPU 的上传

## 对应图片
- ../images/05_gpu_lod.svg

## 讲解目标
把生成好的多层数据转成 texture array 和 GPU mesh。

## 关键代码位置
- src/PlanetRenderer.cpp
- include/PlanetRenderer.h

## 讲解流程
1. setProceduralData() 接收 CPU 数据
2. 检查 face count 和 resolution
3. 创建 height texture array
4. 创建 water / climate / mask texture array
5. 创建材质权重 texture array
6. 上传 baked terrain vertex/index buffers
7. 初始化或刷新海洋、大气相关资源
8. 渲染器标记数据 ready

## 可直接读的口播稿
> 老师好，这一页我讲的是「CPU 数据到 GPU 的上传」。这部分主要想说明：把生成好的多层数据转成 texture array 和 GPU mesh。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 这个模块是 CPU 生成和 GPU 渲染之间的桥。

> CPU 数据本质上是多个数组，而 shader 更适合通过纹理采样。因此代码把六个 cube face 打包成 texture array。

> 高度、水体、温度、湿度、侵蚀、河道和材质权重可以作为不同纹理或不同通道上传。

> 地形 chunk 的顶点和索引则上传到 VBO、IBO、VAO。

> 上传完成后，渲染器每帧就不再重新计算地形，只需要采样纹理和绘制 mesh。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「setProceduralData() 接收 CPU 数据」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「检查 face count 和 resolution」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「创建 height texture array」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「创建 water / climate / mask texture array」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「创建材质权重 texture array」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「上传 baked terrain vertex/index buffers」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「初始化或刷新海洋、大气相关资源」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「渲染器标记数据 ready」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：重点解释 texture array：六个面作为 layer，shader 根据 face/layer 取对应数据。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。