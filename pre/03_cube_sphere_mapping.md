# 03. 六面体星球与接缝处理

## 对应图片
- ../images/02_cube_sphere_mapping.svg

## 讲解目标
用 cube face 组织数据，再映射到球面，降低球面采样复杂度。

## 关键代码位置
- src/PlanetProceduralData.cpp
- include/PlanetProceduralData.h

## 讲解流程
1. 把星球拆成 6 个 cube face
2. 每个 face 使用二维 texel 网格
3. face UV 转换为 cube 坐标
4. cubeSphereDirection() 归一化到球面方向
5. 用方向向量采样噪声和气候函数
6. 跨 face 查找 neighborCell()
7. fixCubeFaceSeams() 多轮边界融合
8. 输出连续的球面高度场

## 可直接读的口播稿
> 老师好，这一页我讲的是「六面体星球与接缝处理」。这部分主要想说明：用 cube face 组织数据，再映射到球面，降低球面采样复杂度。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 直接在球面上做规则网格比较麻烦，所以项目采用 cube-sphere 思路：先把星球拆成六张方形贴图，每张贴图都是普通二维数组。

> 生成时，每个 texel 先得到它在 cube face 上的位置，再归一化成球面方向。之后所有噪声、温度、湿度和水文计算都基于这个球面方向。

> 难点在接缝。如果六个面分别生成，边界很容易出现高度断裂。

> 代码里用 neighborCell() 找到跨面的相邻 texel，再用 fixCubeFaceSeams() 对边界做多轮融合。

> 讲解时可以说：cube-sphere 是数据组织方式，接缝处理保证它最终看起来像一个连续星球。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「把星球拆成 6 个 cube face」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「每个 face 使用二维 texel 网格」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「face UV 转换为 cube 坐标」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「cubeSphereDirection() 归一化到球面方向」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「用方向向量采样噪声和气候函数」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「跨 face 查找 neighborCell()」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「fixCubeFaceSeams() 多轮边界融合」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「输出连续的球面高度场」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：如果被问为什么不用 UV 球，可以回答：cube-sphere 的采样更均匀，适合 texture array 和 chunk 化处理。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。