# 02. 应用状态与生成线程

## 对应图片
- ../images/01_system_overview.svg

## 讲解目标
用三个阶段隔离参数设置、后台生成和实时渲染。

## 关键代码位置
- src/main.cpp

## 讲解流程
1. ProceduralSetup 显示参数设置界面
2. 点击 Generate Planet
3. startPlanetGeneration() 复制当前参数
4. std::async 在后台生成 PlanetProceduralData
5. progressCallback 更新原子进度值
6. 主循环继续刷新进度条
7. future ready 后调用 finishPlanetGeneration()
8. setProceduralData() 上传 GPU 并进入 Render

## 可直接读的口播稿
> 老师好，这一页我讲的是「应用状态与生成线程」。这部分主要想说明：用三个阶段隔离参数设置、后台生成和实时渲染。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 这个模块体现了项目的工程组织方式。生成星球可能比较耗时，所以主线程不直接卡住等待，而是通过 std::async 放到后台执行。

> 后台线程只做 CPU 数据计算，比如高度场、水文和材质权重；它不碰 OpenGL 对象。

> 主循环通过 future 和 atomic 进度值更新 UI，因此生成时仍然可以显示进度条。

> 当 future 完成后，finishPlanetGeneration() 把结果交给 PlanetRenderer，主线程再创建纹理、VBO、IBO 和 VAO。

> 这样既避免 OpenGL 线程问题，也让应用状态从参数设置、生成中、渲染中变得很清楚。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「ProceduralSetup 显示参数设置界面」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「点击 Generate Planet」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「startPlanetGeneration() 复制当前参数」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「std::async 在后台生成 PlanetProceduralData」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「progressCallback 更新原子进度值」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「主循环继续刷新进度条」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「future ready 后调用 finishPlanetGeneration()」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「setProceduralData() 上传 GPU 并进入 Render」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：可以强调：线程拆分不是为了炫技，而是为了避免 UI 假死，并保证 OpenGL 资源只在主线程创建。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。