# 18. 每帧渲染顺序

## 对应图片
- ../images/08_frame_debug.svg

## 讲解目标
把一次 frame 拆成反射折射、地形、海洋、大气和 UI。

## 关键代码位置
- src/PlanetRenderer.cpp
- src/main.cpp

## 讲解流程
1. 主循环处理输入和时间
2. 更新相机矩阵和 renderer 参数
3. 构建可见地形 chunk
4. 构建可见 ocean patch
5. 绘制 reflection/refraction FBO
6. 绘制主场景地形
7. 绘制海洋和大气云层
8. 绘制 ImGui UI 并交换缓冲

## 可直接读的口播稿
> 老师好，这一页我讲的是「每帧渲染顺序」。这部分主要想说明：把一次 frame 拆成反射折射、地形、海洋、大气和 UI。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 这页可以作为前面所有模块的汇总，说明它们在一帧里如何协作。

> 每帧开始先处理输入和时间，更新相机。

> 渲染器根据相机选择可见地形 chunk 和海洋 patch。

> 海洋需要反射和折射，所以主画面前会先做离屏 pass。

> 之后绘制地形、海洋、大气和云层，最后绘制 ImGui。

> 这样讲可以让老师看到：生成模块负责准备数据，渲染模块负责按顺序把数据组织成最终画面。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「主循环处理输入和时间」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「更新相机矩阵和 renderer 参数」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「构建可见地形 chunk」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「构建可见 ocean patch」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「绘制 reflection/refraction FBO」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「绘制主场景地形」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「绘制海洋和大气云层」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「绘制 ImGui UI 并交换缓冲」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：这页适合放在答辩中后段，用来把前面的模块重新串起来。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。