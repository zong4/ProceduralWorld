# 16. Shader 编译与资源管理

## 对应图片
- ../images/05_gpu_lod.svg

## 讲解目标
集中加载 shader、材质资源和 GPU 对象，失败时给出日志。

## 关键代码位置
- src/PlanetRenderer.cpp
- shaders/
- xmake.lua

## 讲解流程
1. xmake 构建时复制 shaders 和 assets
2. PlanetRenderer::initialize() 加载 shader 文件
3. 编译 vertex / fragment / tessellation shader
4. 链接 program 并检查错误
5. 创建 VAO / VBO / texture / FBO
6. 运行时绑定对应 program
7. 设置 uniform 和 texture slot
8. 程序退出时释放 GPU 资源

## 可直接读的口播稿
> 老师好，这一页我讲的是「Shader 编译与资源管理」。这部分主要想说明：集中加载 shader、材质资源和 GPU 对象，失败时给出日志。

> 讲这一页的时候，我会先让大家看对应图片。第一张图适合讲整体感觉，第二张流程图适合按步骤解释。这样听起来会比较顺，不会一下子陷进代码细节里。

> 我先用一句话概括这一页：这个模块负责把前一个环节的数据接过来，做自己的处理，然后把结果交给后面的渲染或调试流程。

> 这个模块偏工程基础，但很重要，因为所有视觉效果都依赖 shader 和 GPU 资源正确加载。

> xmake.lua 会把 shaders 和 assets 复制到输出目录，避免运行时找不到资源。

> PlanetRenderer 初始化时编译并链接多个 shader program，包括地形、海洋、大气和调试 pass。

> 创建纹理、缓冲、FBO 等资源后，每个渲染 pass 会绑定对应 program 和 uniform。

> 如果 shader 编译失败，日志可以帮助定位是哪一行 GLSL 出问题。

> 接下来我按流程图顺着讲。讲的时候可以一边指图，一边读下面这些句子。

> 第 1 步是「xmake 构建时复制 shaders 和 assets」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 2 步是「PlanetRenderer::initialize() 加载 shader 文件」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 3 步是「编译 vertex / fragment / tessellation shader」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 4 步是「链接 program 并检查错误」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 5 步是「创建 VAO / VBO / texture / FBO」。这一步主要是在为后面的步骤做准备。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 6 步是「运行时绑定对应 program」。这里不用展开每一行代码，只要说明它在流程里的作用就可以。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 7 步是「设置 uniform 和 texture slot」。这一步完成后，后面的模块就有了可以继续使用的数据或状态。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 第 8 步是「程序退出时释放 GPU 资源」。讲到这里时，可以指一下图上的箭头，说明流程正在往下一步走。这样讲，老师可以跟着流程图理解每一步为什么接在后面。

> 最后我会这样收尾：所以，这一页不是单独讲一个函数，而是在讲这个功能模块在整个项目里的位置。它的输入是什么，处理了什么，输出又给谁用，这三点说清楚，老师就比较容易理解我的设计思路。

> 如果老师追问，我可以回答：这页不用讲太久，但能说明项目不是只写算法，也处理了实际图形程序的资源生命周期。

## 读的时候怎么把握
- 先用第一段讲清楚“这一页讲什么”。
- 再按流程图一步一步读，不要急着展开全部代码。
- 最后用“输入、处理、输出”三个词收尾。